<p style="color: green"><%= notice %></p>

<%= render @body %>

<div>
  <%= link_to "Edit this body", edit_body_path(@body) %> |
  <%= link_to "Back to bodies", bodies_path %>

  <%= button_to "Destroy this body", @body, method: :delete %>
</div>
